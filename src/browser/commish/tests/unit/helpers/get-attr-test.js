import { getAttr } from 'commish/helpers/get-attr';
import { module, test } from 'qunit';

module('Unit | Helper | get attr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getAttr([42]);
  assert.ok(result);
});

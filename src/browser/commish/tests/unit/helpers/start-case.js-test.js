import { startCaseJs } from 'commish/helpers/start-case.js';
import { module, test } from 'qunit';

module('Unit | Helper | start case.js');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = startCaseJs([42]);
  assert.ok(result);
});
